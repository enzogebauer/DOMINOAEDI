void OrganizeHand (std::list <std::pair <char,char>> &Piece){
    int i, atu, prox;

 
    
    
    
    
    
    
    
    
    
    for(i=0;i<Player[].Piece.size();i++){
        
        atu = Player[].Piece.front(i).first + Player[].Piece.front(i).second;
        prox = Player[].Piece.front(i+1).first + Player[].Piece.front(i+1).second;
        
        if(atu > prox){
        // ent a pedra no local i deve ser trocada pela pedra i+1    
        } 
    }
}